PORT=5000
DATABASE_URL=postgres://postgres:12345@localhost:5432/store_rating
JWT_SECRET=replace_me_with_a_strong_secret

# Deployed frontend (Vercel). IMPORTANT: no trailing slash.
FRONTEND_URL=https://store-rating-platform-five.vercel.app

# Optional: allow multiple origins, comma-separated.
# FRONTEND_URLS=https://store-rating-platform-five.vercel.app,https://another-domain.vercel.app

# Optional: DB retry interval (ms) on cold start
# DB_RETRY_MS=10000

